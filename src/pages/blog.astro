---
import MainLayout from '../layouts/MainLayout.astro';
---

<MainLayout title="Blog | Historias con Alma">
  <section class="flex flex-col items-center justify-center min-h-[70vh] px-6 py-20">
    <div class="max-w-3xl w-full text-center">
      
      <h2 id="typewriter" class="text-3xl md:text-5xl font-mono text-otter-deep leading-relaxed min-h-[150px]">
      </h2>
      
      <div id="blog-content" class="opacity-0 transition-opacity duration-1000 mt-12 text-left">
        <p class="text-slate-500 mb-10 italic text-xl text-center">Historias sobre código, lealtad y el futuro digital.</p>
        
        <div class="grid gap-6">
          
          <a href="/posts/artesania-digital" class="group block">
            <div class="p-8 border-2 border-otter-deep/10 rounded-3xl hover:border-otter-accent transition-all bg-white hover:shadow-xl transform hover:-translate-y-1">
              <div class="flex flex-col md:flex-row md:items-center justify-between gap-4">
                <div>
                  <span class="text-otter-accent font-bold text-xs tracking-widest uppercase mb-2 block">Filosofía Otterock</span>
                  <h3 class="text-2xl font-bold text-otter-deep group-hover:text-otter-accent transition-colors font-display">
                    Más que código: Por qué tu negocio merece "Artesanía Digital"
                  </h3>
                  <p class="text-slate-600 mt-3 font-body leading-relaxed">
                    Vivimos en la era de lo instantáneo. Descubre por qué dejar de buscar la web más rápida y empezar a buscar la web más humana es la mejor estrategia para 2025.
                  </p>
                </div>
                <div class="text-otter-deep/20 group-hover:text-otter-accent text-4xl transition-colors">
                  →
                </div>
              </div>
            </div>
          </a>

          </div>
      </div>
    </div>
  </section>

  <script>
    const text = "En el mundo del código, cada letra cuenta una historia... Esta es la nuestra.";
    const typewriterElement = document.getElementById('typewriter');
    const contentElement = document.getElementById('blog-content');
    let i = 0;

    function type() {
      if (i < text.length) {
        typewriterElement.innerHTML += text.charAt(i);
        i++;
        setTimeout(type, 50); // Velocidad de la letra
      } else {
        // Mostrar el resto del contenido cuando termine de escribir
        contentElement.classList.remove('opacity-0');
      }
    }

    // Iniciar la magia
    window.onload = type;
  </script>
</MainLayout>